<UserControl x:Class="Bee.Modules.ModuleName.Views.ViewA"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Bee.Modules.ModuleName.Views"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             mc:Ignorable="d" 
             xmlns:vm="clr-namespace:Bee.Modules.ModuleName.ViewModels"
             d:DesignHeight="400"
             d:DesignWidth="600"
             prism:ViewModelLocator.AutoWireViewModel="True" 
             d:DataContext="{d:DesignInstance vm:ViewAViewModel}"
             >
    <Grid>
        <DockPanel LastChildFill="True">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="5" >
                <Button Command="{Binding NavigateCommand}" Padding="20 5" CommandParameter="MainOrderView" Margin="5">控制</Button>
                <Button Command="{Binding NavigateCommand}" Padding="20 5" CommandParameter="ProtocolListView" Margin="5">编辑</Button>
            </StackPanel>
            <ContentControl prism:RegionManager.RegionName="TcpControlRegion" Margin="5"  />
        </DockPanel>
    </Grid>
</UserControl>
